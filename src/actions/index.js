import * as t from '../types';

export const increase = () => (dispatch) => {
    dispatch({
        type: t.INCREASE,
        // payload:
    });
};

export const decrease = () => (dispatch) => {
    dispatch({
        type: t.DECREASE,
        // payload:
    });
};